/**
 * No JavaScript, uma função é um bloco de código reutilizável, 
 * que executa uma tarefa ou retorna um valor, e só roda quando é invocada (chamada).
 */
it('Teste inicial', function(){
    
})
    const soma =(a,b) => a + b

        console.log(soma(1,4))